# README 对抗样本生成测试报告（2026-03-04）

## 1. 测试目标与范围
- 目标：执行 `README.md` 中 4 类对抗样本生成样例（TMA / UADA / UPA / UADA-DDP）。
- 限制：全流程仅使用两张显卡，统一采用 `CUDA_VISIBLE_DEVICES=0,1`。
- 方式：先冒烟（`iter=1`）再全量（README/脚本原参数）。

## 2. 测试环境与日志
- 工作目录：`/mnt/home/lvmingyuan/marionette/VLA-attack-AE`
- Conda 环境：`roboticAttack`（Python 3.10.19）
- 数据集目录：`dataset -> /mnt/home/lvmingyuan/data/VLA_Datasets`
- GPU 状态：`nvidia-smi` 返回 `Failed to initialize NVML: Unknown Error`
- 本次日志目录：`logs/attack-readme-2026-03-04_16-30-48`
- 命令退出码汇总：`logs/attack-readme-2026-03-04_16-30-48/45_run_exit_summary.txt`

## 3. 代码与文档修订（已实施）

### 3.1 README DDP 示例修正（两卡 + 正确脚本名）
- 文件：`README.md`
- 修改：
  - `VLAAttacker/UADA_wrapper3_ddp.py` -> `VLAAttacker/UADA_wrapper_ddp.py`
  - `CUDA_VISIBLE_DEVICES=0,1,2,3 --nproc_per_node=4` -> `CUDA_VISIBLE_DEVICES=0,1 --nproc_per_node=2`

### 3.2 UADA/UPA 启动脚本参数去重
- 文件：`scripts/run_UADA.sh`、`scripts/run_UPA.sh`
- 修改：去除重复 `--wandb_project` 参数（保留 1 个，行为不变）。

## 4. 依赖修复尝试与结果

### 4.1 执行命令
1. `conda install -n roboticAttack pytorch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0 pytorch-cuda=12.4 -c pytorch -c nvidia -y`
2. `conda run -n roboticAttack pip install -e .`
3. `conda run -n roboticAttack pip install packaging ninja`
4. `conda run -n roboticAttack pip install "flash-attn==2.5.5" --no-build-isolation`
5. `conda run -n roboticAttack pip install -r experiments/robot/libero/libero_requirements.txt`
6. 额外尝试：`CONDA_NO_PLUGINS=true ...` 与 `--solver classic`

### 4.2 结果
- 以上安装步骤退出码均为 `1`（详见 `logs/attack-readme-2026-03-04_16-30-48/17_install_summary.txt`）。

### 4.3 关键报错与根因
- **Bug-A（Conda 安装失败）**
  - 现象：`PermissionError: [Errno 13] Permission denied`（出现在 conda CUDA 虚拟包探测阶段）。
  - 日志：`11_install_torch.log`、`19_install_torch_no_plugins_classic.log`
  - 根因：当前执行环境对 conda 相关多进程/信号量能力受限，导致 solver 初始化失败。
  - 处理：尝试 `CONDA_NO_PLUGINS=true` + `--solver classic`，仍失败。

- **Bug-B（Pip 安装失败）**
  - 现象：`Failed to establish a new connection: [Errno -2] 未知的名称或服务`
  - 日志：`12_pip_install_editable.log`、`13_install_packaging_ninja.log`、`14_install_flash_attn.log`、`15_install_libero_requirements.log`
  - 根因：网络/DNS 访问 PyPI 受阻，无法下载依赖。
  - 处理：已重试，仍失败。

## 5. 四类样例执行结果（命令 -> 报错 -> 根因 -> 修复 -> 验证）

### 5.1 TMA
- 冒烟命令：`31_smoke_tma`
- 全量命令：`41_full_tma_script`
- 报错：`ModuleNotFoundError: No module named 'torch'`
- 根因：核心依赖未安装成功。
- 修复动作：执行第 4 节依赖修复流程。
- 修复后验证：仍失败（依赖安装被外部环境阻塞）。

### 5.2 UADA
- 冒烟命令：`32_smoke_uada`
- 全量命令：`42_full_uada_script`
- 报错：`ModuleNotFoundError: No module named 'torch'`
- 根因：核心依赖未安装成功。
- 修复动作：执行第 4 节依赖修复流程。
- 修复后验证：仍失败（依赖安装被外部环境阻塞）。

### 5.3 UPA
- 冒烟命令：`33_smoke_upa`
- 全量命令：`43_full_upa_script`
- 报错：`ModuleNotFoundError: No module named 'torch'`
- 根因：核心依赖未安装成功。
- 修复动作：执行第 4 节依赖修复流程。
- 修复后验证：仍失败（依赖安装被外部环境阻塞）。

### 5.4 UADA-DDP（两卡）
- 原 README 命令复现：`34_smoke_ddp_readme_original`
  - 报错：`torchrun: 未找到命令`
  - 根因：torch 未安装导致 `torchrun` 不存在。
- 原 README 脚本路径核验：`35_smoke_ddp_wrapper3_pathcheck`
  - 报错：`can't open file ... UADA_wrapper3_ddp.py: [Errno 2] No such file or directory`
  - 根因：README 示例脚本名错误。
- 修正后命令：`36_smoke_ddp_corrected`、`44_full_ddp_corrected`
  - 报错：仍为 `torchrun: 未找到命令`
  - 根因：依赖安装失败，未具备运行前提。
- 修复动作：
  1) 文档修正为 `UADA_wrapper_ddp.py` + 两卡参数；
  2) 依赖安装尝试。
- 修复后验证：文档路径问题已修复；运行仍被环境依赖阻塞。

## 6. 验收结论
- 已完成：
  - 4 个样例均执行并留存日志/退出码证据。
  - README DDP 命令错误（脚本名 + GPU 数）已修订。
  - UADA/UPA 启动脚本重复参数已修订。
- 未完成（受外部阻塞）：
  - 未能进入真实训练循环并产出攻击样本；主要阻塞为 conda/pip 依赖安装失败。

## 7. 后续建议（落地顺序）
1. 在可联网且 conda 多进程能力正常的机器上重跑第 4 节安装步骤。
2. 安装成功后验证：
   - `conda run -n roboticAttack python -c "import torch,transformers,wandb;print(torch.__version__)"`
   - `conda run -n roboticAttack torchrun --help`
3. 再执行本文中的 4 项冒烟与全量命令（继续保持 `CUDA_VISIBLE_DEVICES=0,1`）。
